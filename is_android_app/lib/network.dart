

import 'package:is_android_app/ip_adress.dart';

class Network {
  late final IPAddress networkAddress;
  late final IPAddress maxAddress;

  Network(InetAddress address, short mask) {
    late IPAddress ip = new IPAddress()
  }
}